-- 시드가 「컨셉 다듬기를 지나 확정된 것」인지 표시한다.
--
-- 왜 필요했나. 시장 인터뷰는 이 시드에서 여섯 칸을 꺼내 응답자에게 보일 자극을 만든다.
-- 그런데 시드는 **두 번** 발급된다 — 사업안을 고른 직후 한 번(시장조사가 이것을 먹는다),
-- 그리고 다듬기가 끝나고 「이 컨셉으로 확정하기」를 누를 때 한 번. 앞의 것으로 조사가 나가면
-- 사용자는 **다듬기 전 사업안**을 소비자에게 물어보게 되고, 화면에는 그 사실이 안 보였다.
--
-- 두 시드는 형태가 완전히 같아 구별할 근거가 없었다. 그 근거를 여기 남긴다.
--
-- ⚠ 시각 비교(다듬기 최종행 updated_at vs 시드 finalized_at)로 대신하지 않는다. 같은 트랜잭션
--    안에서 두 값이 붙고, 재시도·시계 오차에 판정이 흔들린다. 「지났다/안 지났다」는 사실이지
--    계산이 아니다.
--
-- 기존 행은 FALSE 로 남는다. 그 프로젝트들은 사업 검증 화면에서 「이 컨셉으로 확정하기」를
-- 다시 누르면 TRUE 인 시드가 새로 발급된다 — 막다른 길이 되지 않게 재확정 경로를 같이 열었다
-- (ConceptPortfolioSelectionService.finalizeMarketSeed).

ALTER TABLE market_analysis_seed_snapshots
    ADD COLUMN refinement_applied BOOLEAN NOT NULL DEFAULT FALSE;
